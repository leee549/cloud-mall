<?xml version="1.0" encoding="utf-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="cn.lhx.mall.product.dao.SkuSaleAttrValueDao">
    <resultMap id="skuSaleAttrValue" type="cn.lhx.mall.product.entity.SkuSaleAttrValueEntity" >
        <result property="id" column="id"/>
        <result property="skuId" column="sku_id"/>
        <result property="attrId" column="attr_id"/>
        <result property="attrName" column="attr_name"/>
        <result property="attrValue" column="attr_value"/>
        <result property="attrSort" column="attr_sort"/>
    </resultMap>
    <resultMap id="skuItemSaleAttrVo" type="cn.lhx.mall.product.vo.SkuItemSaleAttrVo">
        <result property="attrId" column="attr_id"/>
        <result property="attrName" column="attr_name"/>
        <collection property="attrValues" ofType="cn.lhx.mall.product.vo.AttrValueWithSkuIdVo">
            <result property="attrValue" column="attr_value"/>
            <result property="skuIds" column="sku_ids"/>
        </collection>
    </resultMap>

    <select id="getSaleAttrsBySpuId" resultMap="skuItemSaleAttrVo">
        SELECT ssav.attr_id, ssav.attr_name,ssav.attr_value,
               GROUP_CONCAT(DISTINCT info.sku_id) sku_ids
        FROM `pms_sku_info` info
        left JOIN pms_sku_sale_attr_value ssav on ssav.sku_id=info.sku_id
        where spu_id=#{spuId}
        GROUP BY ssav.attr_id, ssav.attr_name,ssav.attr_value
    </select>


</mapper>
